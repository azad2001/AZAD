<!DOCTYPE html>
<html lang="fa">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>پنل ۳گانه AZAD</title>
<style>
body {
font-family: Arial, sans-serif;
margin: 0;
background: #111;
color: #fff;
display: flex;
flex-direction: column;
align-items: center;
}
header {
background: #222;
padding: 10px;
width: 100%;
text-align: center;
font-size: 1.2em;
}
section {
max-width: 600px;
width: 100%;
padding: 10px;
box-sizing: border-box;
}
canvas {
display: block;
margin: 0 auto;
}
svg {
display: block;
margin: 20px auto;
}
button {
background: #444;
color: #fff;
border: none;
padding: 6px 12px;
margin-top: 10px;
cursor: pointer;
}
button:hover {
background: #666;
}
</style>
</head>
<body>
<header>🌐 پنل AZAD — 3D + Chart.js + SVG</header>

<section>
<h2>نمایشگر سه‌بعدی</h2>
<div id="three-container" style="width:100%;height:300px;"></div>
<button id="spinBtn">چرخش کره</button>
</section>

<section>
<h2>چارت زنده</h2>
<canvas id="myChart" width="600" height="300"></canvas>
</section>

<section>
<h2>انیمیشن SVG</h2>
<svg width="200" height="200" viewBox="0 0 200 200">
<circle id="pulse" cx="100" cy="100" r="40" stroke="#0ff" stroke-width="4" fill="none"/>
</svg>
</section>

<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/examples/js/controls/OrbitControls.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.0/chart.umd.min.js"></script>
<script>
// === Three.js ===
let scene = new THREE.Scene();
let camera = new THREE.PerspectiveCamera(75, document.getElementById('three-container').clientWidth / 300, 0.1, 1000);
let renderer = new THREE.WebGLRenderer({antialias:true});
renderer.setSize(document.getElementById('three-container').clientWidth, 300);
document.getElementById('three-container').appendChild(renderer.domElement);

let geometry = new THREE.SphereGeometry(1, 32, 32);
let material = new THREE.MeshStandardMaterial({color: 0x00ffcc, wireframe: false});
let sphere = new THREE.Mesh(geometry, material);
scene.add(sphere);

let light = new THREE.PointLight(0xffffff, 1);
light.position.set(5,5,5);
scene.add(light);

let controls = new THREE.OrbitControls(camera, renderer.domElement);
camera.position.z = 3;

let spinning = false;
document.getElementById('spinBtn').onclick = () => { spinning = !spinning; };

function animate3D(){
requestAnimationFrame(animate3D);
if(spinning) sphere.rotation.y += 0.01;
controls.update();
renderer.render(scene, camera);
}
animate3D();

// === Chart.js ===
let ctx = document.getElementById('myChart').getContext('2d');
let chartData = {
labels: [],
datasets: [{
label: 'داده زنده',
borderColor: '#0ff',
backgroundColor: 'rgba(0,255,255,0.2)',
data: []
}]
};
let myChart = new Chart(ctx, {
type: 'line',
data: chartData,
options: {responsive: true, scales:{x:{ticks:{color:'#fff'}},y:{ticks:{color:'#fff'}}},
plugins:{legend:{labels:{color:'#fff'}}}}
});
setInterval(()=>{
let now = new Date().toLocaleTimeString();
let val = Math.floor(Math.random()*100);
chartData.labels.push(now);
chartData.datasets[0].data.push(val);
if(chartData.labels.length>10){
chartData.labels.shift();
chartData.datasets[0].data.shift();
}
myChart.update();
}, 2000);

// === SVG Animation ===
let pulse = document.getElementById('pulse');
let grow = true;
setInterval(()=>{
let r = parseFloat(pulse.getAttribute('r'));
if(grow){ r+=1; if(r>=60) grow=false; }
else { r-=1; if(r<=40) grow=true; }
pulse.setAttribute('r', r);
}, 50);
</script>
</body>
</html>